# Quarto documentation (quartodocs/) build artifacts
quartodocs/_site/
quartodocs/.quarto/
quartodocs/reference/

# Jupyter Notebook
.ipynb_checkpoints

# Figure grid script/notebook -- experimental, not synced/canonical, kept
# local only (see fig_grid.pdf/png in the manuscript repo for the actual
# paper figure)
scripts/figures/fig_grid.py
scripts/figures/fig_grid.ipynb

__pycache__/
*.xml
.DS_Store

# packaging build artifacts
*.egg-info/
build/
dist/

# Boundary files for figure generation (scripts/figures/fig_grid.py,
# fig_bihar_floods.py) -- not vendored, see scripts/figures/_boundaries.py
resources/boundaries/

# Pipeline outputs, caches, and scratch/log dirs -- not code, not vendored
output/
resources/slope/
scratch_quebracho_test/
scripts/logs/

# Pipeline-produced data files, wherever they land -- rasters, NetCDF
# baselines/slope, mosaic VRTs, and the scene-lookup JSON caches
# (*_dry_aoi_scene.json etc.). Does NOT cover the small, deliberately
# committed vector resources (resources/india_utm_fishnet*.gpkg) since
# those aren't matched by any pattern below.
*.tif
*.tiff
*.nc
*.vrt
*_dry_aoi_scene.json
*_dry_scene_aoi.json
*_wet_aoi_scene.json
*_wet_scene_aoi.json
